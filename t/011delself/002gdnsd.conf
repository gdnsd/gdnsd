
options => {
  listen => @dns_lspec@
  http_listen => @http_lspec@
  dns_port => @dns_port@
  http_port => @http_port@
  zones_dir = "@cfdir@/002zones"
  realtime_stats = true
  include_optional_ns = true
}

zones => {
    example.com => {},
    subz.example.com => {}
}
