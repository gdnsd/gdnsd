
options => {
  listen => @dns_lspec@
  http_listen => @http_lspec@
  dns_port => @dns_port@
  http_port => @http_port@
  zones_dir = "@cfdir@/001zones"
  realtime_stats = true
  include_optional_ns = true
}

zones => {
    subz.example.com => {},
    example.com => {}
}
